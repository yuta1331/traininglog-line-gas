# ドメイン用語

このプロジェクトで使う言葉の定義。コード上の識別子・コミットメッセージ・issue はここの語彙に合わせる。

網羅は狙わない。曖昧さで手が止まった語だけを、止まったときに足す。

## トレーニング記録

1セット分の記録。ユーザーID・日付・店舗・種目・重量・回数・トップセットの7項目からなり、スプレッドシートの1行に対応する。

LINE のメッセージ1通は複数のトレーニング記録に展開される（種目行1つにつきセット数ぶん）。

## トップセット

1つの種目の中で、0回のセットを除いて最も重い、かつその重量の中で最も回数が多いセット。`parseTrainingLog` が種目ごとに最大1つ選ぶ。同点の場合は先に現れたセットが勝つ。0回のセットしか無い種目行にはトップセットが無い。

## 許可ユーザー

UserList シートに列挙された LINE ユーザーID。ここに無いユーザーからのメッセージは処理も返信もしない。

現在この判定はリクエストボディ由来の `userId` の照合のみで、Webhook の署名検証は行っていない（[ADR-0002](docs/adr/0002-webhook-signature-verification-not-possible-on-gas.md)）。

## メッセージハンドラ

テキストメッセージ1通を受け取り、**処理結果**を返す module（`src/services/messageHandler.ts`）。

内部で「許可ユーザー判定 → 自動既読 → コマンド判別 → 記録の保存 or JSON書き出し」を行う。この順序自体が仕様であり（既読を付けるのは許可ユーザーのメッセージだけ）、順序を module の内側に閉じ込めることがこの module の存在理由。

LINE の Webhook ペイロードの形は知らない。`doPost` が整形した `{userId, text, markAsReadToken}` だけを受け取る。

> 「intake（取り込み）」という語は使わない。この module は JSON書き出しや「返信しない」判定も担っており、取り込みより広い。

## 処理結果

メッセージハンドラの戻り値。「何が起きたか」を表す判別可能なユニオンで、**LINE に返す文言は含まない**。

文言への変換は `doPost` 側の文言化関数が担う。処理結果と文言は N:1 で対応してよい（例：保存失敗と想定外エラーは同じ文言）。

## 記録ストア

トレーニング記録をスプレッドシートに追記する module。列順の知識・`LockService` による排他・`setValues` をここに閉じ込める。

排他が守っている不変条件は「`getLastRow` と `setValues` の間に他の実行が割り込まないこと」であり、これは記録ストアの関心。ネットワークI/Oを排他の内側に入れてはならない（全 Webhook 処理が直列化する）。

## JSON書き出し

`json書き出し` というメッセージを受けて、スプレッドシートの全記録を日付・店舗・種目でグループ化した JSON にし、Google Drive に保存してダウンロードURLを返す機能。同名ファイルは置き換える。

## 自動既読

受信したメッセージに既読を付ける機能。LINE 公式アカウントマネージャーでチャットがONのときだけ Webhook に `markAsReadToken` が含まれ、OFFのときは何もしない。

許可ユーザーのメッセージにのみ既読を付ける。
